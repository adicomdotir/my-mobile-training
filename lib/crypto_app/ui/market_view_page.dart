import 'package:flutter/material.dart';

class MarketViewPage extends StatelessWidget {
  const MarketViewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Market View Page'),
    );
  }
}
